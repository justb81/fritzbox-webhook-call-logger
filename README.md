# fritzbox-webhook-call-logger
This application connects to the calllog port of the fritzbox and sends webhook event for every call.

## Environment
```
FRITZBOX_HOST = "fritz.box"
FRITZBOX_PORT = 1012
```